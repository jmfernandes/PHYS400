def get_list():
	mylist  = raw_input("Enter a  list: ").strip()
	if mylist.startswith('['):
		mylist  = mylist[1:]
	else:
		print "lists must start with a [!"
		return None
	if mylist.endswith(']'):
		mylist  = mylist[:-1]
	else:
		print "you forgot the ] on the end!"
		return None
	return mylist.split(",")

x = get_list()	#creates a list containing string versions of the numbers you entered
a=[]		#creates an empty array	
for i in x:
	c = int(i)			#turns the list of strings into integers
	for j in range(2,c):
		n = c % j
		if n == 0:
			a.insert((j-2),1)
		else:
			if not a: 
				break
			else:
				a=[]		#if the factors of the number are divisible,

		break				#1 gets deposited into array a. if not, array
						#a will be empty
	if not a:
  		print '{} is a prime number'.format(c)	#if array is empty, this is printed
	else:
		print '{} is not a prime number'.format(c)  #if array is not empty, this is printed.
